import { MirroringImageDirective } from './mirroring-image.directive';

describe('MirroringImageDirective', () => {
  it('should create an instance', () => {
    // const directive = new FallbackImageDirective();
    // expect(directive).toBeTruthy();
  });
});
